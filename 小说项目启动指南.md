# 🚀 AI 小说写作助手 - 启动指南

## 📋 快速启动（推荐）

### 方式一：双击启动（最简单）
1. 打开文件夹：`C:\Users\Zhanghw\ai-coding\AI-Novel-Writing-Assistant\`
2. 找到文件：**`启动小说项目.bat`**
3. **双击运行**
4. 等待服务启动完成

### 方式二：PowerShell 启动
1. 右键 `启动小说项目.ps1`
2. 选择"使用 PowerShell 运行"

### 方式三：命令行启动
```bash
cd C:\Users\Zhanghw\ai-coding\AI-Novel-Writing-Assistant
pnpm dev
```

---

## 🌐 访问地址

启动成功后，在浏览器中访问：

- **前端界面**：http://localhost:5173
- **后端 API**：http://localhost:3000

---

## 📦 项目结构

```
AI-Novel-Writing-Assistant/
├── client/          # 前端（React/Vue）
├── server/          # 后端（Node.js）
├── shared/          # 共享代码
├── desktop/         # 桌面应用（Electron）
└── package.json     # 项目配置
```

---

## 🔧 常用命令

### 开发模式
```bash
pnpm dev              # 启动前后端（推荐）
pnpm dev:server       # 只启动后端
pnpm dev:client       # 只启动前端
pnpm dev:desktop      # 启动桌面应用
```

### 数据库管理
```bash
pnpm db:migrate       # 运行数据库迁移
pnpm db:seed          # 填充测试数据
pnpm db:studio        # 打开数据库管理界面
```

### 构建打包
```bash
pnpm build            # 构建前后端
pnpm build:desktop    # 构建桌面应用
```

---

## ⚙️ 环境配置

### 首次运行前需要：

1. **安装 Node.js**（版本 20.19+ 或 22.12+ 或 24+）
   - 下载：https://nodejs.org/

2. **安装 pnpm**
   ```bash
   npm install -g pnpm
   ```

3. **配置环境变量**
   - 复制 `.env.example` 为 `.env`
   - 填写必要的配置（API 密钥等）

---

## 🐛 常见问题

### 问题1：端口被占用
**错误信息**：`Port 3000 is already in use`

**解决方法**：
```bash
# 查找占用端口的进程
netstat -ano | findstr :3000

# 结束进程（替换 PID）
taskkill /PID <进程ID> /F
```

### 问题2：依赖安装失败
**解决方法**：
```bash
# 清理缓存
pnpm store prune

# 重新安装
rm -rf node_modules
pnpm install
```

### 问题3：数据库错误
**解决方法**：
```bash
# 重置数据库
pnpm db:migrate
pnpm db:seed
```

---

## 💡 开发提示

### 热重载
- 前端和后端都支持热重载
- 修改代码后会自动刷新，无需重启

### 日志查看
- 日志文件位置：`.logs/` 目录
- 实时查看：启动脚本会显示所有日志

### 停止服务
- 在启动窗口按 `Ctrl + C`
- 或直接关闭命令行窗口

---

## 🎯 与番茄小说集成

### 工作流程
1. **启动小说项目**（本项目）
   - 用于创作、编辑、管理小说内容
   
2. **启动浏览器控制**（browser-control）
   - 用于自动化发布到番茄小说平台

### 推荐工作流
```
创作小说（本项目） 
    ↓
导出章节内容
    ↓
使用浏览器控制发布到番茄小说
```

---

## 📞 需要帮助？

- 查看项目文档：`README.md`
- 查看任务列表：`TASK.md`
- 查看代理说明：`AGENTS.md`

---

## 🔗 相关项目

- **浏览器控制系统**：`C:\Users\Zhanghw\browser-control\`
- **番茄小说发布指南**：`C:\Users\Zhanghw\browser-control\番茄小说发布指南.md`

---

**祝你写作愉快！✍️**
